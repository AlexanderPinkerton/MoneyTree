{
    "buildCommand": "turbo build --filter=@moneytree/next",
    "outputDirectory": "apps/next/.next",
    "installCommand": "yarn install",
    "framework": "nextjs",
    "devCommand": "yarn workspace @moneytree/next dev",
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./apps/next",
    "git": {
        "deploymentEnabled": {
            "main": false,
            "development": false
        }
    }
  }
}
